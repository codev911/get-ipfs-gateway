{
  "name": "get-ipfs-gateway",
  "description": "Get Public IPFS gateway url easily",
  "author": "codev911",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "type": "module",
  "files": [
    "dist"
  ],
  "license": "MIT",
  "scripts": {
    "build": "rimraf dist && tsup",
    "format": "npx prettier --write lib/*.ts"
  },
  "devDependencies": {
    "prettier": "^3.2.4",
    "rimraf": "^5.0.5",
    "tslib": "^2.6.2",
    "tsup": "^8.0.1",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "axios": "^1.6.5"
  }
}
